# spam-detection-
to find and detect fake mails in your inbox
echo "# spam-detection-" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Aadarshthatikonda/spam-detection-.git
git push -u origin main
